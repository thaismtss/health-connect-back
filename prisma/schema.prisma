// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Services {
  id              String            @id @default(uuid())
  name            String
  description     String
  slug            String            @unique
  ServicesOnUsers ServicesOnUsers[] @relation(name: "ServicesToServicesOnUsers")

  @@map("services")
}

model ServicesOnUsers {
  id        String    @id @default(uuid())
  userId    String
  serviceId String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  removedAt DateTime?
  user      User      @relation(name: "ServicesOnUsersToUser", fields: [userId], references: [id])
  service   Services  @relation(name: "ServicesToServicesOnUsers", fields: [serviceId], references: [id])

  @@map("services_on_users")
}

model User {
  id        String            @id @default(uuid())
  services  ServicesOnUsers[] @relation(name: "ServicesOnUsersToUser")
  email     String            @unique
  name      String?
  password  String
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt
  glycemics Glycemic[]        @relation(name: "GlycemicToUser")

  @@map("users")
}

model Glycemic {
  id        String   @id @default(uuid())
  value     String
  fasting   Boolean
  status    String
  userId    String
  user      User     @relation(name: "GlycemicToUser", fields: [userId], references: [id])
  createdAt DateTime

  @@map("glycemics")
}
